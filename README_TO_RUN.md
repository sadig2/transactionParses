# to run the code
# create and environment:

    python3 -m venv env
# activate it
    source env/bin/activate

# install packages
    pip install -r requirements.txt

# run the code

    python3 main.py

